# =========================
# Side-Markdown env sample
# =========================
#
# Renderer (Vite) build-time env:
# - Copy this file to `.env.local` and fill in the values
# - Restart Vite to take effect
# - Only variables prefixed with `VITE_` are exposed to the renderer.
#
# Main (Electron) runtime env:
# - This project does NOT auto-load `.env` via dotenv for the main process.
# - For Electron main vars below, set them via your shell `export ...`
#   or inject via `cross-env` when launching.
#

# -------------------------
# Analytics (renderer only)
# -------------------------
#
# The app automatically chooses analytics provider based on user's timezone:
#   - China timezone (Asia/Shanghai, etc.) → Baidu Tongji
#   - Other timezones → Google Analytics
#
# Both providers can be configured; the app will use the appropriate one.

# Enable/disable analytics (default: true)
VITE_ANALYTICS_ENABLED=true

# Region mode: auto / cn / non-cn (default: auto)
#   - auto: detect by OS timezone (recommended)
#   - cn: force China mode (use Baidu)
#   - non-cn: force non-China mode (use Google)
VITE_ANALYTICS_REGION=auto

# Provider override: auto / baidu / ga / both (default: auto)
#   - auto: choose by region + available ids (recommended)
#   - baidu: force Baidu only
#   - ga: force Google only
#   - both: send events to both providers
VITE_ANALYTICS_PROVIDER=auto

# Debug logging in renderer console (default: false)
# Set to true to see detailed analytics logs in DevTools
VITE_ANALYTICS_DEBUG=false

# -------------------------
# Baidu Tongji (百度统计)
# -------------------------
# Get your site ID from: https://tongji.baidu.com/
# Format: 32-character hex string
VITE_BAIDU_SITE_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# -------------------------
# Google Analytics 4
# -------------------------
# Get your Measurement ID from GA4 Admin > Data Streams
# Format: G-XXXXXXXXXX
VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# ⚠️ IMPORTANT: GA4 API Secret (REQUIRED for Electron/desktop apps)
#
# Without this, Google Analytics will NOT work in Electron due to
# file:// protocol limitations. The app uses GA4 Measurement Protocol
# which requires an API Secret.
#
# How to get your API Secret:
#   1. Go to https://analytics.google.com/
#   2. Click Admin (gear icon, bottom left)
#   3. In Property column, click "Data Streams"
#   4. Select your web data stream
#   5. Scroll down to "Measurement Protocol API secrets"
#   6. Click "Create" and copy the secret value
#
# Format: alphanumeric string (e.g., AbCdEfGhIjKlMnOp)
VITE_GA_API_SECRET=xxxxxxxxxxxxxxxx


# -------------------------
# Electron main (dev/debug)
# -------------------------
# Trace analytics-related network requests in Electron main (dev/debug only):
# Run with: SIDE_MARKDOWN_TRACE_ANALYTICS=1 npm run dev
# SIDE_MARKDOWN_TRACE_ANALYTICS=1
# ANALYTICS_TRACE=1
# ELECTRON_TRACE_ANALYTICS=1

# Dev-only: override Electron userData dir (when not packaged)
# ELECTRON_USER_DATA_DIR=/absolute/path/to/.electron-user-data

# Dev-only: open DevTools automatically on launch
# ELECTRON_OPEN_DEVTOOLS=1

# Dev server URL injected by `npm run dev` script (no need to set manually)
# VITE_DEV_SERVER_URL=http://localhost:5173


# -------------------------
# Release / publishing
# -------------------------
# Used by electron-builder when publishing to GitHub Releases (NOT loaded automatically)
# GH_TOKEN=YOUR_GITHUB_PAT
